# Pac-Man (Ghost AI)

> Ghost-mode AI as nested state machines — four ghosts each running the same FSM, with HSM-inherited frighten handling and a top-level mode cycle.

This game's controller is three composed state machines: **GhostGame** (the orchestrator), **Ghost** (four parameterized instances running the same machine), and **GhostPen**.

_Article in progress — write me._
